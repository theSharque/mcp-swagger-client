# MCP Swagger

[![npm version](https://img.shields.io/npm/v/mcp-swagger-client.svg)](https://www.npmjs.com/package/mcp-swagger-client)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

> Model Context Protocol (MCP) сервер для доступа и поиска по Swagger/OpenAPI документации

**Умный MCP-сервер**, который подключается к Swagger/OpenAPI спецификациям с интеллектуальным кешированием и мощными возможностями поиска.

📦 **Установка**: `npm install -g mcp-swagger-client` или используйте через npx
🌐 **npm**: https://www.npmjs.com/package/mcp-swagger-client
🔗 **GitHub**: https://github.com/theSharque/mcp-swagger-client

## Обзор

Получайте доступ и исследуйте вашу API-документацию через Swagger/OpenAPI спецификации с поиском на основе AI, детальной информацией об endpoint'ах и автоматическим разрешением схем.

## Возможности

- 🔍 **Умный поиск**: Поиск API endpoints по пути, методу, описанию, тегам и параметрам
- 📋 **Детальная информация**: Получение полных деталей endpoint включая параметры, схемы запроса/ответа и коды ошибок
- 📦 **Инспектор моделей**: Исследование моделей данных с полной информацией о свойствах и ограничениях
- 📝 **Список всех API**: Получение полного обзора всех доступных endpoints с группировкой по тегам
- 🧪 **Выполнение запросов**: Встроенный HTTP-клиент для тестирования API прямо из MCP
- 🚀 **Интеллектуальное кеширование**: Локальное кеширование с валидацией через HEAD-запросы (ETag/Last-Modified)
- 🔐 **Множество методов аутентификации**: Поддержка Bearer токенов, Basic Auth и cookies
- 📚 **Поддержка OpenAPI**: Работает как со Swagger 2.0, так и с OpenAPI 3.0+
- 🔗 **Разрешение ссылок**: Автоматически разрешает `$ref` ссылки на схемы

## Быстрый старт

### Для пользователей (использование npm пакета)

```bash
# Установка не требуется - используйте напрямую в Cursor/Claude Desktop
# Просто настройте как описано в разделе Интеграция ниже
```

### Для разработчиков

1. Клонируйте репозиторий:
```bash
git clone https://github.com/theSharque/mcp-swagger-client.git
cd mcp-swagger-client
```

2. Установите зависимости:
```bash
npm install
```

3. Соберите проект:
```bash
npm run build
```

## Конфигурация

Настройте сервер используя переменные окружения:

### Обязательные

- `MCP_SWAGGER_URL` - URL вашей Swagger/OpenAPI спецификации (например, `https://api.example.com/swagger.json`)

### Опциональная аутентификация

Выберите один из следующих методов:

- `MCP_SWAGGER_TOKEN` - Bearer токен для аутентификации
- `MCP_SWAGGER_USER` + `MCP_SWAGGER_PASSWORD` - Basic аутентификация
- `MCP_SWAGGER_COOKIES` - Строка cookies для аутентификации на основе сессий

**Приоритет**: Token → Basic Auth → Cookies

## Интеграция

### Cursor IDE

1. Откройте Cursor Settings → Features → Model Context Protocol
2. Нажмите кнопку "Edit Config"
3. Добавьте одну из конфигураций ниже

#### Вариант 1: Через npx (Рекомендуется)

Автоматически устанавливается из npm registry:

```json
{
  "mcpServers": {
    "swagger": {
      "command": "npx",
      "args": ["-y", "mcp-swagger-client"],
      "env": {
        "MCP_SWAGGER_URL": "https://petstore.swagger.io/v2/swagger.json"
      }
    }
  }
}
```

С аутентификацией:

```json
{
  "mcpServers": {
    "swagger": {
      "command": "npx",
      "args": ["-y", "mcp-swagger-client"],
      "env": {
        "MCP_SWAGGER_URL": "https://api.example.com/swagger.json",
        "MCP_SWAGGER_TOKEN": "your_bearer_token_here"
      }
    }
  }
}
```

#### Вариант 2: Через npm link (Разработка)

Для локальной разработки с живыми изменениями:

```json
{
  "mcpServers": {
    "swagger": {
      "command": "mcp-swagger-client",
      "env": {
        "MCP_SWAGGER_URL": "https://api.example.com/swagger.json"
      }
    }
  }
}
```

Требуется: `cd /path/to/mcp-swagger-client && npm link -g`

#### Вариант 3: Прямой путь

```json
{
  "mcpServers": {
    "swagger": {
      "command": "node",
      "args": ["/path/to/mcp-swagger-client/dist/index.js"],
      "env": {
        "MCP_SWAGGER_URL": "https://api.example.com/swagger.json"
      }
    }
  }
}
```

#### Для локального API

```json
{
  "mcpServers": {
    "swagger": {
      "command": "node",
      "args": ["/path/to/mcp-swagger-client/dist/index.js"],
      "env": {
        "MCP_SWAGGER_URL": "http://localhost:8080/v3/api-docs"
      }
    }
  }
}
```

### Claude Desktop

Редактируйте `~/Library/Application Support/Claude/claude_desktop_config.json` (macOS) или `%APPDATA%\Claude\claude_desktop_config.json` (Windows):

```json
{
  "mcpServers": {
    "swagger": {
      "command": "npx",
      "args": ["-y", "mcp-swagger-client"],
      "env": {
        "MCP_SWAGGER_URL": "https://petstore.swagger.io/v2/swagger.json"
      }
    }
  }
}
```

С аутентификацией:

```json
{
  "mcpServers": {
    "swagger": {
      "command": "npx",
      "args": ["-y", "mcp-swagger-client"],
      "env": {
        "MCP_SWAGGER_URL": "https://api.example.com/swagger.json",
        "MCP_SWAGGER_TOKEN": "your_bearer_token_here"
      }
    }
  }
}
```

### Continue.dev

Редактируйте `.continue/config.json`:

```json
{
  "mcpServers": {
    "swagger": {
      "command": "npx",
      "args": ["-y", "mcp-swagger-client"],
      "env": {
        "MCP_SWAGGER_URL": "https://petstore.swagger.io/v2/swagger.json"
      }
    }
  }
}
```

### Примеры аутентификации

#### Bearer Token

```json
{
  "env": {
    "MCP_SWAGGER_URL": "https://api.example.com/swagger.json",
    "MCP_SWAGGER_TOKEN": "your_bearer_token_here"
  }
}
```

#### Basic Auth

```json
{
  "env": {
    "MCP_SWAGGER_URL": "https://api.example.com/swagger.json",
    "MCP_SWAGGER_USER": "username",
    "MCP_SWAGGER_PASSWORD": "password"
  }
}
```

#### Cookies

```json
{
  "env": {
    "MCP_SWAGGER_URL": "https://api.example.com/swagger.json",
    "MCP_SWAGGER_COOKIES": "session=abc123; token=xyz789"
  }
}
```

## Инструменты

### 1. `search-api`

Поиск API endpoints по ключевому слову. Возвращает минимальную информацию для быстрого просмотра.

**Входные данные:**
- `query` (string) - Поисковый запрос

**Выходные данные:**
- `results` - Массив найденных endpoints
  - `path` - Путь endpoint
  - `method` - HTTP метод
  - `description` - Краткое описание
- `total` - Количество результатов

**Пример:**
```typescript
// Поиск endpoints связанных с пользователями
{
  "query": "user"
}

// Возвращает:
{
  "results": [
    {
      "path": "/api/users",
      "method": "GET",
      "description": "Получить всех пользователей"
    },
    {
      "path": "/api/users/{id}",
      "method": "GET",
      "description": "Получить пользователя по ID"
    }
  ],
  "total": 2
}
```

### 2. `get-api-details`

Получить полные детали о конкретном API endpoint.

**Входные данные:**
- `path` (string) - Путь API endpoint (например, `/api/users/{id}`)
- `method` (string) - HTTP метод (GET, POST, PUT, DELETE, и т.д.)

**Выходные данные:**
Полная информация об endpoint включая:
- Краткое описание и детальное описание
- Все параметры (path, query, header, cookie)
- Схема тела запроса
- Все схемы ответов с кодами статусов
- Требования безопасности
- Примеры (если доступны)

**Пример:**
```typescript
// Получить детали endpoint пользователя
{
  "path": "/api/users/{id}",
  "method": "GET"
}

// Возвращает:
{
  "path": "/api/users/{id}",
  "method": "GET",
  "operationId": "getUserById",
  "summary": "Получить пользователя по ID",
  "description": "Получает детальную информацию о конкретном пользователе",
  "tags": ["Users"],
  "parameters": [
    {
      "name": "id",
      "in": "path",
      "required": true,
      "schema": { "type": "integer" },
      "description": "ID пользователя"
    }
  ],
  "responses": {
    "200": {
      "description": "Успешный ответ",
      "content": {
        "application/json": {
          "schema": { "$ref": "#/components/schemas/User" }
        }
      }
    },
    "404": {
      "description": "Пользователь не найден"
    }
  }
}
```

### 3. `get-model-details`

Получить полную информацию о модели данных/схеме.

**Входные данные:**
- `modelName` (string) - Название модели (например, `User`, `Pet`, или `#/components/schemas/User`)

**Выходные данные:**
Полная схема модели включая:
- Тип и описание
- Все свойства с типами и ограничениями
- Обязательные поля
- Вложенные модели (разрешенные `$ref`)
- Примеры и правила валидации

**Пример:**
```typescript
// Получить детали модели User
{
  "modelName": "User"
}

// Возвращает:
{
  "name": "User",
  "type": "object",
  "description": "Информация об аккаунте пользователя",
  "required": ["id", "email", "username"],
  "properties": {
    "id": {
      "type": "integer",
      "format": "int64",
      "description": "Уникальный идентификатор пользователя"
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "Email адрес пользователя"
    },
    "username": {
      "type": "string",
      "minLength": 3,
      "maxLength": 50,
      "description": "Уникальное имя пользователя"
    }
  }
}
```

### 4. `list-all-api`

Получить полный список всех доступных API endpoints.

**Входные данные:**
Не требуются (инструмент без параметров)

**Выходные данные:**
- `endpoints` - Массив всех endpoints
  - `path` - Путь endpoint
  - `method` - HTTP метод
  - `operationId` - ID операции
  - `summary` - Краткое описание
  - `tags` - Теги/категории
- `total` - Общее количество endpoints
- `groupedByTag` - Endpoints сгруппированные по тегам

**Пример:**
```typescript
// Получить все endpoints
{}

// Возвращает:
{
  "endpoints": [
    {
      "path": "/api/users",
      "method": "GET",
      "operationId": "getAllUsers",
      "summary": "Получить всех пользователей",
      "tags": ["user-controller"]
    },
    // ... остальные endpoints
  ],
  "total": 35,
  "groupedByTag": {
    "user-controller": [...],
    "Authentication": [...],
    "feature-controller": [...]
  }
}
```

### 5. `check-api`

Выполнить реальный HTTP-запрос к любому API endpoint из OpenAPI спецификации.

**Входные данные:**
- `path` (string, обязательный) - Путь API endpoint (например, `/api/users`, `/api/features/123`)
- `method` (string, обязательный) - HTTP метод (GET, POST, PUT, DELETE, PATCH, и т.д.)
- `body` (any, опциональный) - Тело запроса (будет преобразовано в JSON если объект)
- `queryParams` (object, опциональный) - Query параметры как пары ключ-значение
- `headers` (object, опциональный) - Дополнительные HTTP заголовки

**Выходные данные:**
- `status` - HTTP код статуса
- `statusText` - Текст статуса
- `headers` - Заголовки ответа
- `data` - Данные ответа (JSON или text)
- `requestUrl` - Полный URL запроса
- `executionTime` - Время выполнения в миллисекундах

**Возможности:**
- Автоматическое разрешение base URL из OpenAPI спецификации
- Автоматическая аутентификация (Bearer token, Basic auth, или Cookies из конфигурации)
- Автоматический парсинг JSON/text ответов
- Измерение времени выполнения

**Примеры:**

GET запрос:
```typescript
{
  "path": "/api/features",
  "method": "GET",
  "headers": {
    "Cookie": "X-Auth=your_jwt_token"
  }
}
```

POST запрос с телом:
```typescript
{
  "path": "/api/features",
  "method": "POST",
  "body": {
    "authorId": 1,
    "functionalAreaIds": [11],
    "quarter": "Q1",
    "year": 2026,
    "summary": "Новая функция",
    "description": "Описание новой функции"
  },
  "headers": {
    "Cookie": "X-Auth=your_jwt_token"
  }
}
```

PUT запрос:
```typescript
{
  "path": "/api/features/15",
  "method": "PUT",
  "body": {
    "id": 15,
    "authorId": 1,
    "functionalAreaIds": [11],
    "quarter": "Q2",
    "year": 2026,
    "summary": "Обновленная функция"
  },
  "headers": {
    "Cookie": "X-Auth=your_jwt_token"
  }
}
```

DELETE запрос:
```typescript
{
  "path": "/api/features/15",
  "method": "DELETE",
  "headers": {
    "Cookie": "X-Auth=your_jwt_token"
  }
}
```

**Важно**: Этот инструмент выполняет РЕАЛЬНЫЕ запросы к API. Будьте осторожны с операциями POST/PUT/DELETE!

## Как работает кеширование

Сервер реализует интеллектуальное кеширование для минимизации API запросов:

1. **Первый запрос**: Скачивает и кеширует OpenAPI спецификацию в `~/.mcp-swagger-client/cache/`
2. **Последующие запросы**:
   - Выполняет быстрый HEAD запрос для проверки изменений спецификации
   - Сравнивает заголовки `ETag` и `Last-Modified`
   - Использует кешированную версию если не изменилась
   - Скачивает свежую копию если изменилась

**Преимущества:**
- Быстрое время ответа для повторных запросов
- Минимальная нагрузка на ваш API
- Всегда актуально при изменении спецификации
- Работает оффлайн если кеш существует

**Расположение кеша**: `~/.mcp-swagger-client/cache/`

## Использование

### Режим разработки

Запуск с горячей перезагрузкой:
```bash
npm run dev
```

### Продакшн режим

Запуск сервера:
```bash
npm start
```

### MCP Inspector

Отладка и тестирование сервера с помощью MCP Inspector:
```bash
npm run inspector
```

Это открывает MCP Inspector UI где вы можете интерактивно тестировать все инструменты.

## Разработка

### Структура проекта

```
mcp-swagger-client/
├── src/
│   ├── index.ts          # MCP сервер с регистрацией инструментов
│   ├── swagger-client.ts # Основной клиент для API операций
│   ├── cache.ts          # Механизм кеширования с HEAD валидацией
│   ├── parser.ts         # OpenAPI парсер с разрешением $ref
│   └── types.ts          # TypeScript определения типов
├── dist/                 # Скомпилированный вывод
├── package.json
├── tsconfig.json
├── README.md
└── README_RU.md
```

## Примеры использования

### 1. Исследование API
```
"Найди все endpoints связанные с аутентификацией"
→ search-api: { query: "auth" }
```

### 2. Разработка интеграции
```
"Покажи мне детали endpoint создания пользователя"
→ get-api-details: { path: "/api/users", method: "POST" }
```

### 3. Понимание моделей данных
```
"Какие поля есть в модели User?"
→ get-model-details: { modelName: "User" }
```

### 4. Обработка ошибок
```
"Какие ошибки возвращает endpoint логина?"
→ get-api-details: { path: "/api/auth/login", method: "POST" }
```

### 5. Получение полного списка API
```
"Покажи все доступные endpoints"
→ list-all-api: {}
```

### 6. Тестирование API
```
"Создай новую функцию в API"
→ check-api: {
    path: "/api/features",
    method: "POST",
    body: { ... },
    headers: { "Cookie": "X-Auth=token" }
  }
```

## Решение проблем

### Ошибка: "MCP_SWAGGER_URL environment variable is required"

Убедитесь что вы установили переменную окружения `MCP_SWAGGER_URL` с вашим Swagger/OpenAPI URL.

### Ошибки аутентификации

- Проверьте что ваши учетные данные корректны
- Проверьте что метод аутентификации соответствует вашему API (token, basic auth, или cookies)
- Убедитесь что токен/учетные данные имеют необходимые права

### Проблемы с кешем

Если вы видите устаревшие данные, можете вручную очистить кеш:

```bash
rm -rf ~/.mcp-swagger-client/cache/
```

### Проблемы с версией OpenAPI

Сервер поддерживает как Swagger 2.0, так и OpenAPI 3.0+. Если вы сталкиваетесь с проблемами парсинга, проверьте что ваша спецификация валидна используя:
- [Swagger Editor](https://editor.swagger.io/)
- [OpenAPI Validator](https://validator.swagger.io/)

### Перезапуск MCP-сервера

После изменения конфигурации или обновления кода, перезапустите MCP-серверы:

**В Cursor:**
- `Ctrl+Shift+P` (или `Cmd+Shift+P` на Mac)
- Введите "Developer: Reload Window"
- Нажмите Enter

Или через настройки:
- Settings → Features → Model Context Protocol
- "Restart MCP Servers"

## Лицензия

MIT

## Автор

theSharque

## Дополнительная информация

Этот MCP-сервер предоставляет AI-ассистентам полный доступ к вашей API документации:
- 🔍 Интеллектуальный поиск по всем полям
- 📖 Детальная документация с разрешенными схемами
- 📋 Полный инвентарь API
- 🧪 Встроенный HTTP-клиент для тестирования

Больше не нужно переключаться между документацией, Postman и IDE - всё доступно прямо в вашем AI-ассистенте!
